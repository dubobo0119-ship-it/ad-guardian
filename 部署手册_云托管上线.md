# 广告卫士 · 微信云托管上线手册（路线 A）

> 目标：把小程序从"电脑调试版"变成"微信里人人可用的正式版"。
> 全程不用买服务器、不用买域名、不用备案。预计耗时：注册约 30 分钟 + 部署约 1 小时（含等审核）。

---

## 第 0 步：准备清单

| 事项 | 说明 |
|---|---|
| 一个没注册过公众平台的邮箱 | QQ 邮箱即可 |
| 管理员的微信 + 身份证 | 注册个人主体小程序要实名验证 |
| 已装好的微信开发者工具 | 就是你正在用的这个 |

---

## 第 1 步：注册小程序账号（免费，约 15 分钟）

1. 浏览器打开 **mp.weixin.qq.com** → 右上角「立即注册」→ 选「**小程序**」
2. 填邮箱 → 收激活邮件 → 选主体类型「**个人**」
3. 管理员信息：微信扫码 + 身份证实名（按提示走）
4. 注册完成后登录后台，左侧「**开发 → 开发设置**」里复制 **AppID**（形如 `wx` 开头）

> 拿到 AppID 后：开发者工具顶部「详情 → 基本信息」把测试号换成你的 AppID。

---

## 第 2 步：开通微信云托管（免费额度，约 10 分钟）

1. 小程序后台左侧菜单「**云托管**」（或直接访问 cloud.weixin.qq.com 用小程序账号登录）
2. 按提示开通（新用户自动获得 **3 个月免费额度**）
3. 记下环境 ID（形如 `xxx-xxxxxx`）

---

## 第 3 步：上传后端镜像（约 15 分钟，不用装 Docker）

1. 云托管控制台 → 「**新建服务**」，服务名填 `adguardian`
2. 部署方式选「**代码包部署**」
3. 把 `ad-guardian` 目录打包成 zip：
   - 注意要包含：`api.py`、`adguardian/` 文件夹、`Dockerfile`、`.dockerignore`
   - **不要**包含：`wxapp/`、`data/`、`examples/`（其实 .dockerignore 已兜底排除）
4. 上传 zip → 监听端口填 **80** → 点「部署」
5. 等构建完成（约 2~5 分钟），状态变「运行中」后，控制台里点「测试」访问 `/api/health`，应返回 `{"status":"ok"}`

---

## 第 4 步：开通公网访问 + 配置域名白名单（约 10 分钟）

1. 云托管控制台 → 服务详情 → 「**访问设置**」→ 开启「**默认公网访问**」（自动生成一个 HTTPS 域名，免备案免证书）
2. 复制这个域名（形如 `adguardian-xxxx.ap-shanghai.run.tcloudbase.com`）
3. 去小程序后台「**开发 → 开发设置 → 服务器域名 → request 合法域名**」，把该域名（带 `https://`）添加进去
   - 上传体验版/正式版必须配这个；本地开发时开发者工具可继续用不校验

---

## 第 5 步：把小程序的接口地址换成云域名（1 行代码）

打开 `wxapp/pages/index/index.js` 和 `wxapp/pages/kb/kb.js`，把第一行：

```js
const BASE = 'http://127.0.0.1:8000';
```

改成第 4 步拿到的云域名：

```js
const BASE = 'https://你的云托管域名';
```

保存 → 开发者工具编译 → 全流程回归测试一遍（识别 / 文书 / 反馈）。

---

## 第 6 步：上传代码 → 提审 → 发布（等审核 1~7 天）

1. 开发者工具右上角「**上传**」→ 版本号 `1.0.0` → 备注"首个版本"
2. 小程序后台「**管理 → 版本管理**」→ 开发版本 → 「**提交审核**」
3. 填写审核信息（见下方合规要点）
4. 审核通过后点「**发布**」→ 微信里搜索小程序名字就能找到了

### 审核合规要点（重要，决定能否过审）

- **小程序名称**：如"广告卫士小助手"，别用"12321""官方""维权中心"等字样
- **服务类目**：个人主体选「**工具 → 信息查询**」或「生活服务」
- **简介话术**：定位为"**帮助整理解读广告法规知识、辅助用户自行向官方渠道反馈**"
- **不要出现**的表述："代举报""代投诉""受理投诉"（我们只是辅助整理材料，受理方是官方渠道——这是产品的合规生命线）

---

## 等审核期间：体验版先给亲友用

1. 版本管理 → 开发版本 → 「**设为体验版**」
2. 后台「**管理 → 成员管理 → 体验成员**」添加家人朋友的微信号（个人主体上限 15 人）
3. 他们扫码即可使用完整版（云托管后端 + AI 看图）——正好推进用户验证

---

## 已知的小限制（后面迭代解决）

| 限制 | 原因与对策 |
|---|---|
| 意见反馈存在容器本地文件 | 容器重启会清空；正式版迭代时换成云托管自带 MySQL（免费额度内） |
| 密钥写死在镜像里 | 正式发布前去智谱重建密钥，改到云托管「环境变量」配置 |
| 无缩容到 0 | 免费额度期内保持最小 1 实例即可；计费后如心疼可配缩容策略 |

---

## 卡住了怎么办

每一步如果报错或界面和手册不符，截图发我，我看着带你过。
